---
title: "Batch Converting Image Files (Heic To Png For Exam"
category: "general-linux"
tags: ["batch", "converting", "image", "files", "heic"]
---

* Download the `tar.gz` file.
* Extract and within the directory, run `make && make install`
* You will need the `make` and `gcc` packages installed beforehand.
	* In my case for openSUSE, I needed `gimp-devel` installed as well.
* The package is then automatically