---
title: "Using !$ Takes The Last Arguments From The Previou"
category: "general-linux"
tags: ["takes", "last", "arguments", "previou"]
---

For example, running `mkdir ~/<directory>` and then `cp !$` would then mean `cp ~/<directory>`