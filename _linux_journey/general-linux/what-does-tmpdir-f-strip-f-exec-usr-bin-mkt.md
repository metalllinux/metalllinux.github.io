---
title: "What Does Tmpdir = ${F Strip ${F Exec Usr Bin Mkt"
category: "general-linux"
tags: ["tmpdir", "strip", "exec", "usr", "bin"]
---

It captures the path of the command `mktemp` for the temporary directory. The `f:strip` part removes any trailing or leading whitespaces from the `mktemp` command.