---
title: "When Creating An Ssh Keypair On Aws"
category: "networking"
tags: ["networking", "when", "creating", "ssh", "keypair"]
---

* Make sure `chmod 600` is set on the private key:
```
chmod 600 ~/ssh_keys/.ssh/aws-711387140773-main.pem
```
* Log in as the `rocky` user.