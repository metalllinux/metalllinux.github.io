---
title: "How Use Tcp Dump"
category: "networking"
tags: ["networking", "tcp", "dump"]
---

sudo tcpdump host <IP> && port <port>
sudo tcpdump src <IP> or dst <IP>
sudo tcpdump not port 22
sudo tcpdump -vv host && port
Shows the above more verbosely.