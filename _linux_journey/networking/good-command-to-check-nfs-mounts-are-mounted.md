---
title: "Good Command to Check NFS Mounts are Mounted Correctly:"
category: "networking"
tags: ["networking", "good", "command", "check", "nfs"]
---

# Good Command to Check NFS Mounts are Mounted Correctly:

On the client node that the NFS server node shared the NFS directory with:

```
showmount -e <NODE_HOSTNAME>
```

